import { globalStyle } from '@vanilla-extract/css';

globalStyle('html', {
  width: '100%',
  height: '100%',
});

globalStyle('body', {
  width: '100%',
  height: '100%',
  color: 'white',
});

globalStyle('#storybook-root', {
  width: '100%',
  height: '100%',
});
